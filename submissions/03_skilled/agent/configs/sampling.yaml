# This agent should be terse. Output tokens are the expensive half of the
# bill, so the cap is deliberately low -- the agent has nothing long to say,
# it just calls tools.
#
# claude-opus-4-5 rejects `temperature` in generate_content_config entirely,
# so it's omitted here (see submissions/03_skilled_opus/configs/sampling.yaml
# for the same pattern, used earlier this session for local sanity-checking).
max_output_tokens: 2048
